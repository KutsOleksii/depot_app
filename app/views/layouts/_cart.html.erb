<% Rails.logger.debug("______________________WE ARE HERE in _cart.html.erb______________________") %>
<% if cart and not cart.line_items.empty? %>
  <div id="cart" class="bg-white rounded p-2">
    <%= render cart %>
  </div>
<% else %>
  <div id="cart"></div>
<% end %>
